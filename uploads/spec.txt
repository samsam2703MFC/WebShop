PHASE 1 — FOUNDATION   Database, auth, design system — nothing else works without these [1.1] Branding — CSS Variables &amp; Global Design System Apply brand colors, fonts, and logo consistently throughout the entire store — both Webshop and Office Shop. No element should fall outside the brand system. All color, font, and logo values driven exclusively by CSS variables from the Brand admin panel — zero hardcoded styles anywhere in the front-end codebase Brand palette: Blanc / Noir / Ruby Red (#8D1D2C) / Abricot Pastel (#F2C9A0) — enforced via CSS variables Typography: Cormorant Garamond + Outfit — defined in Brand admin, applied globally Covers: Webshop, Office Shop, all modals, checkout, cards, badges, headers, footers [1.2] SSO — Single Sign-On between PWA and Webshop The end-user is already logged in on the existing PWA. When they click the webshop icon inside the PWA, they must arrive on the webshop already authenticated — no second login required. On redirect from the PWA to the webshop, pass a short-lived signed JWT token as a URL parameter — single-use, expires in 60 seconds — never reusable The webshop receives the token, validates it against the shared auth backend, and creates an authenticated session automatically If the token is invalid or expired: user lands as a guest and is prompted to log in normally Admin configuration: dedicated SSO settings panel — shared secret / API key — toggle enabled/disabled — token expiry duration configurable (default: 60 seconds) Security: token is signed — cannot be forged — HTTPS required on both sides — token tied to user ID and timestamp — replay attacks not possible [1.3] Shop Location — Align Edit with Create The Edit Shop page must use the exact same location fields, structure, and validation logic as the Create Shop page. A shop's location must be fully editable after creation — no workarounds, no manual database hacks All fields, validation rules, and options related to location must be identical between Create and Edit No risk of breaking the existing shop setup when saving location changes If a field exists on Create, it must exist on Edit — and vice versa [1.4] Digiteal — Shop Payment Configuration In the admin panel, under each shop's settings, add a dedicated Digiteal Payment Configuration section containing all fields required to identify the shop as a Digiteal entity and authenticate API calls: Company Identifier ( company_id ) — the Digiteal company ID assigned to this franchise shop API Key ( api_key ) — the secret key used to authenticate Digiteal API requests IBAN — the bank account to which payments are credited for this shop Creditor Name — the legal name of the creditor as registered in Digiteal Creditor Reference Prefix — optional prefix for structured payment references per order (e.g.  LAT-HALLE- ) Webhook URL — auto-generated from the shop's unique identifier — read-only — copyable in one click — to be pasted in the Digiteal dashboard Mode — toggle: Test / Live PEPPOL ID (optional) — if the shop is registered on PEPPOL for B2B invoice routing Visibility toggle:  Payment enabled  (ON / OFF) — if OFF: payment step hidden in the shop front end — configurable message shown instead (e.g. &quot;Online payment temporarily unavailable&quot;) — message editable from the same panel All fields stored securely per shop — never exposed on the frontend — used server-side only Connection test button — verifies credentials against Digiteal API — result shown inline (✓ Connected / ✗ Failed with error message) [1.5] Translation Management (Global) &amp; Language Assignment (Per Shop) Separate language management into two distinct sections: Translation Management — Global (superadmin / franchisor only): One central translation table covering all translatable strings: UI labels, buttons, forms, modals, error messages, checkout copy, product names, category names, descriptions, footer content, countdown text, empty state messages, campaign copy Translations done once globally — not per shop Table columns: Key | Default language | Language 2 | Language 3 | ... — inline editable Status indicator per row: Complete / Partial / Missing — filterable by status, language, module CSV export: one row per key, one column per language CSV import with diff preview before confirming — shows new / updated / unchanged rows Language management table: name, locale code, active toggle, default language flag — add / remove languages Language Assignment — Per Shop (franchisee back office): Each franchise shop independently selects its default/preferred language and its active optional languages Language switcher appears on the storefront only if more than one language is active for that shop PHASE 2 — ADMIN &amp; DATA CORE   Back office configuration and ERP data structures — before touching the storefront [2.1] Product Admin — ID Display &amp; Variant / Bundle Logic Product ID display: Display  id_product  as a read-only field on every product card in the admin panel — visible at all times — small and discreet — copyable on click Variant / Bundle logic (admin + front-end): Variant dropdown only appears if the product has a bundle assigned If no bundle assigned: dropdown is hidden — applies consistently in product creation, editing, and on the front-end product card and info modal [2.2] Category &amp; Collection — Illustrations &amp; Visual Settings Category &amp; subcategory illustrations: Add ability to upload or link an illustration directly on each category card — not in brand settings Illustration must be strictly 1:1 ratio (square) — enforced on upload — accept file upload or external URL The image is used as the category badge illustration on the storefront Collection visual settings (create / edit collection): A collection acts as a global badge to select a seasonal or thematic assortment of products — cross-category by nature Illustration — upload or URL — strictly 1:1 ratio Badge background color — color picker Badge border / line color — color picker Each collection badge has its own distinct visual identity, separate from standard category badge style [2.3] Bundle &amp; Menu Module — ERP Integration Bundles and menus are defined in the Franchise Buddy ERP and pushed to the webshop admin via API — the webshop does not create them. Bundle logic: When a product belongs to a bundle, clicking (+) reveals the bundle context instead of adding directly to cart Product-level bundle: shows quantity needed to trigger the bundle — OR if first product already in cart, second offered at -X% automatically — discount applied at cart level, clearly labelled Category-level bundle: shows that this product is part of a bundle with products from another category — customer prompted to complete the bundle Menu logic: Menu structure from ERP: Product category 1 + Product category 2 = €X — optionally + Product category 3 = €X2 Customer selects one product per category slot — each slot shows available in-stock products for that category and selected date Price updates dynamically as slots are filled &quot;Add menu to cart&quot; button active only when all required slots are filled — optional slots clearly marked and skippable Admin — Menu &amp; Bundle View: Menus and bundles from ERP displayed in a dedicated read-only panel in admin Franchisee can toggle a menu or bundle ON/OFF for their store — cannot edit the structure (ERP-owned) API: GET /api/v1/menus, GET /api/v1/bundles [2.4] Composite Product (Skladane) — Configurable Tray/Plate All parameters come from the ERP and are pushed to the admin via API — the webshop does not define them. Front-end behavior — on the product card (+): A modal opens showing the list of available sub-products — each line: Category — Product name / (+) button The (+) increments by the configured step (e.g. by 4 or by 5 — set in ERP per sub-product) Running total shows units selected vs maximum allowed — total price updates dynamically &quot;Add to cart&quot; button inactive until total equals the configured maximum — activates when reached If customer tries to exceed maximum: (+) is blocked with a visual indicator Admin — Composite Product View (read-only): Franchisee can toggle ON/OFF for their store — cannot edit the structure (ERP-owned) API: GET /api/v1/composite-products [2.5] Product Availability — Lead-Time &amp; Cut-Off Logic (Webshop) Per-product settings (back office): Available today — based on real stock quantity Order D+1 — 1 day lead time / Order D+2 — 2 days lead time Cut-off hour — e.g. 18:00 — latest time to order for the next available day Front-end logic: Stock: &quot;Available today&quot; products only show if selected date = today AND stock &gt; 0 Lead-time: D+1 from next day, D+2 from day after next Cut-off: if BEFORE cut-off → D+1 = tomorrow / D+2 = day after. If AFTER cut-off → D+1 shifts to D+2 / D+2 shifts to D+3 Example: Wednesday 17:00 (before cut-off) → D+1 = Thursday, D+2 = Friday. Wednesday 19:00 (after cut-off) → D+1 = Friday, D+2 = Saturday Only show products matching the selected date, stock, and lead-time+cut-off constraints — hide or disable all non-eligible products [2.6] Office Shop — Working Days Configuration (Per Store) In the Office Shop admin panel, add a working days configuration — a simple weekly toggle (Monday to Sunday): Only configured working days are selectable in the date picker for Office Shop orders Non-working days are greyed out and blocked in the date picker Default: Monday to Friday — weekends off [2.7] Delivery Rounds — Shop Badge on Tournée Tile In the tournée list view, add a badge on each tournée tile clearly indicating which franchise shop it belongs to — shop name, visually distinct, always visible on the tile without needing to open the round. [2.8] Campaign Management — Two Types Type 1 — Traffic Campaign: Admin setup: campaign name, start/end date, linked store(s), products with price override and qty, pickup dates and slots (fixed), unique campaign URL, returning customer discount rate (e.g. 30%) Path A — New customer: arrives via campaign link → registration → store selection → campaign products → fixed slot → order confirmed → PDF + QR code by email or pushed to PWA Path B — Existing customer (id_customer matched): campaign badge changes color — clicking confirms products added to cart with X% discount automatically — proceeds to slot selection → checkout Discount applies exclusively to campaign products — same mechanic (slots, store, qty) for both paths New vs returning customer paths tracked separately in campaign analytics view Type 2 — Promotion Campaign: Admin setup: campaign name, start/end date, linked store(s), products with promotional price, qty available, date range, product card badge (label + color configurable) Promotional price and badge appear automatically on product card during campaign period — normal ordering flow Badge disappears automatically when campaign ends or stock depleted Campaign Admin List View: List: type badge (Traffic / Promotion), status (Active / Scheduled / Ended), store, date range Actions: duplicate, pause, archive per campaign [2.9] Voucher Management Voucher code (manual or auto-generated), discount type (% or fixed €), discount value Applicable to: all products / specific products / specific categories / specific collections Valid from / valid until — max uses global and per customer — minimum order amount Applicable shop: Webshop only / Office Shop only / both At checkout: voucher code input field — real-time validation — discount applied immediately If invalid, expired, or already used: clear error message — if minimum not reached: message with remaining amount Admin list view: code, discount, status (Active / Expired / Depleted), usage count, store, date range Actions: edit, deactivate, duplicate Integration with Direct Link Generator: voucher code embeddable in the direct link — pre-applied at checkout automatically [2.10] Direct Link Generator — Admin Tool Build a pre-parameterized URL that lands the customer directly on the shop with specific context pre-loaded: Parameters: shop (dropdown), picking date (date picker), product (optional — searchable dropdown — displayed as Category — Product name), voucher code (optional) Generated URL format:  https://shop.latelierbyfr/store?shop=halle&amp;date=2025-12-20&amp;product=123&amp;voucher=NOEL25 Storefront behavior: shop pre-selected, date pre-selected, product highlighted or modal opened automatically, voucher pre-applied at checkout Admin UX: visual builder via dropdowns — Generate button — one-click copy — QR code downloadable for print or digital use [2.11] Post-Payment Upsell — Weekend, Long Weekend &amp; Bank Holiday After payment confirmation, before the thank-you screen, display a one-click upsell page — only if today falls within the configured active days: Offer: a special pack pulled dynamically from the ERP — single card: pack name, photo, description, price — one-click confirm — no new form Optional discount if taking immediately — or possibility to order for someone else Admin configuration: Toggle: enabled/disabled — active days: configurable day by day (default Mon–Fri) Special pack: linked to a basket from the ERP — pulled dynamically Bank holidays: manually add specific dates with custom label (e.g. &quot;Easter Special&quot;) and specific pack — falls back to default weekend pack if none assigned Long weekend auto-detection: if bank holiday on Thursday or Tuesday → Friday or Monday auto-flagged as trigger day (toggle to enable/disable) Date range supported (e.g. 3-day Easter weekend) Logic: If today NOT within active days: skip — go directly to thank-you If customer already has a pending weekend order for that shop: skip — no duplicate Applies to both Webshop and Office Shop post-payment flows [2.12] Multi-Day Order Module — Grouped Weekly Ordering with Discount Tiers Add a dedicated badge/button in the shop (distinct visual — not a standard category badge) that opens a multi-day ordering flow: Customer flow: Customer clicks the multi-day badge Available days shown (based on shop working days and Office Shop delivery schedule) Customer selects the days they want to order for (e.g. Monday, Wednesday, Friday) For each day: product grid filtered by that day's availability (stock, D+1/D+2, cutoff logic applied per day) Discount tier updates dynamically as days are added (e.g. &quot;3 days selected — you save 15%&quot;) Delivery mode determined automatically: linked to office client → Office Shop tournée delivery / not linked → click &amp; collect Full grouped order summary reviewed: one line per day with basket, delivery mode, and date Single checkout — single Digiteal payment — discount applied to total Each day generates an independent order in the back office — all linked under the same grouped order reference Discount tiers (admin — per franchise shop): Configurable table: number of days → discount % — franchisee adds or removes rows freely Discount applies to the total of all grouped orders Tiers displayed to the customer in real-time as they add days Back office: Toggle: multi-day ordering enabled/disabled — controls badge visibility on the storefront Tier configuration table: number of days → discount % Grouped orders appear linked under a single reference — each day-order visible as a sub-order Delivery mode per sub-order shown: Office Delivery or Click &amp; Collect Grouped order reference included in the confirmation email and PDF [2.13] API Documentation Panel — All Admin Modules For every module in the admin and franchisee back office, add a collapsible API Documentation panel at the bottom of each section: Title: &quot;API Reference — [Module Name]&quot; — collapsed by default — read-only Per endpoint: method badge (GET / POST / PUT / PATCH / DELETE — color coded), endpoint URL, short description, request body example (JSON), response example (JSON), required headers:  Authorization: Bearer {token} ,  Content-Type: application/json REST conventions: GET list / GET by id / POST create / PUT full update / PATCH partial / DELETE Modules to cover: Categories &amp; Subcategories, Products, Shops, Office Clients, End-Users, Delivery Rounds, Orders, Checkout &amp; Payment, Collection Hours &amp; Slots, Discount / Cross-shop settings, Menus, Bundles, Composite Products, Campaigns, Vouchers, Direct Links, Multi-Day Orders, Translations PHASE 3 — STOREFRONT UX   Browsing, filtering, product cards — depends on Phases 1 &amp; 2 [3.1] Date Picker — Navbar (Critical) Immediately below the shop selector dropdown in the navbar, add a compact inline date picker: No products are shown until a date is selected For the Webshop: date determines availability based on stock, D+1/D+2 lead-time, and cut-off hour logic For the Office Shop: date must match an available delivery round for the customer's linked office client Only valid/available dates are selectable — past dates and dates with no availability are greyed out or blocked If selected date has no available products or no delivery round: clear message shown — no broken grid Once a date is selected: product grid loads instantly for that date and shop context [3.2] Category &amp; Subcategory Filter — Full Audit &amp; Fix All behavior must be inline — no page reloads, no redirects at any step: Category click: shows all products in that category including all subcategory products — subcategory badges appear inline below Subcategory click: filters grid to show only products from that specific subcategory — grid updates in place Click active category again (subcategory active): resets to all products in that category — subcategory row remains visible but none selected Click active category again (no subcategory active): resets to full product list for that shop Only categories and subcategories with at least one active product toggled ON for that shop are shown — empty ones never appear Active/selected badge visually clear — not just underlined Filter state independent between Webshop and Office Shop On mobile: horizontal scroll — no wrapping — no overflow [3.3] Collection Badges — Same Row as Categories Collection badges on the same horizontal row as category badges — appended at the end — visually distinct via their own background and border color Tapping a collection badge filters the grid to all products in that collection — regardless of category Collection and category filters are mutually exclusive If a collection badge is active: subcategory row does not appear — collections are cross-category by nature [3.4] Product Card — Layout, Info Modal &amp; (+) Logic Card layout — strict two-line grid structure below the photo: Line 1: Product name (left aligned) / Price (right aligned) — same line Line 2: Allergen pictograms small and round (left side) / (ℹ) info icon + (+) add to basket button (extreme right) — same line Allergen pictograms visible directly on the card (+) button logic: Standard product: adds directly to cart — no modal — cart badge increments — side cart does NOT open Bundle product: (+) reveals bundle context Menu product: (+) opens menu builder modal Composite product: (+) opens composite tray builder modal (ℹ) icon: opens a lightweight modal — product name, max 2 lines description, variant dropdown if bundle assigned, allergen pictograms at bottom Grid: 6 per row desktop — 3 tablet — 2 mobile — CSS Grid via CSS variable — no horizontal overflow at any breakpoint [3.5] Multi-Day Order Badge — Storefront Entry Point A dedicated badge/button — distinct from category badges and collection badges — visible in the shop when multi-day ordering is enabled for that store. Clicking it opens the multi-day ordering flow as described in [2.12]. [3.6] Sign-Out Behavior When a customer signs out: Office Client icon loses its green/red status — reverts to default state Cart/basket icon is no longer visible Both elements are only shown to logged-in users [3.7] Office Client Icon — Switching Shops When a customer switches to a different franchise shop: If the user has an office client linked to a different store: show the entry greyed out with notice — &quot;This office client is only available at [shop name]&quot; Greyed-out entry is read-only — cannot be used to place an order in the current shop Below: option to link to a new office client for the current shop — via code or 3-step registration depending on admin setting PHASE 4 — OFFICE SHOP LAYER   Registration, countdown, empty states — office shop fully functional before checkout [4.1] Office Client Registration — Inline Form If a logged-in end-user has no linked office client and clicks on the Office Shop, display a clean inline form (not a modal): Fields: Company name, Address, Postal code On submit: request sent to franchisee for approval — user sees confirmation message — no access until approved [4.2] Office Shop — Countdown &amp; Empty States Live countdown in Office Shop header: linked to customer's tournée cutoff — format: &quot;Order before 10:30 — 1h 42min remaining&quot; — minimal, consistent with existing CSS Countdown hits zero: order button disabled — next delivery window message shown No active round assigned: no countdown shown No linked office client + no products available: clean centered modal — invites user to click the Office Client icon — no form, no broken grid PHASE 5 — CHECKOUT &amp; PAYMENT   Last — depends on everything above [5.1] Invoice &amp; VAT Validation Flow (Webshop &amp; Office Shop) If a customer requests an invoice (via the &quot;Request an invoice&quot; checkbox at checkout): VAT number field appears — required — accepts any EU VAT format Checked in real-time against EU VIES system (https://ec.europa.eu/taxation_customs/vies/) If VIES valid: standard PDF invoice generated and sent — full company details If VIES invalid or unavailable: simple A4 sale attestation PDF generated automatically Per order in back office: VAT number stored — VIES result stored — document type recorded (invoice / attestation) [5.2] Checkout — Webshop &amp; Office Shop (Definitive Logic) Webshop — Click &amp; Collect — Logged-in: Name, surname, email, phone pre-filled — picks date and 15-minute collection slot — pays online Zero address fields — no store selector — store shown as read-only info block: name, address, Google Maps link, phone, email Step indicator: Addresses → Delivery → Payment → Review — Ruby Red, thin rounded pill badges, completed steps slightly darker Webshop — Click &amp; Collect — Guest: Fields: first name, last name, email, phone only — then collection slot — then payment Office Shop checkout: Everything pre-filled and read-only: name, surname, email, phone, company, delivery slot, delivery address Two interactions only: checkbox &quot;Request an invoice&quot; + Confirm &amp; Pay → payment portal One scroll, one confirm — nothing else Multi-Day Order checkout: Full grouped summary displayed: one line per day — basket, delivery mode, date Discount tier applied and shown clearly on the total Single Digiteal payment covering all days Linked flow — Office Shop + Webshop Add-On: While building the Office Shop cart: minimalist callout in Webshop colors — inline nudge, not a blocking popup — message editable from back office If accepted: second cart in Webshop colors — in-stock products only — out-of-stock not shown 5% discount applied automatically — rate configurable from franchisee back office At checkout: both carts shown together — all data pre-filled — single confirm — one payment — one fluid flow [5.3] Post-Payment Flow Immediately after payment confirmation — before thank-you screen: If today matches a configured upsell trigger day: upsell page appears with the occasion-specific pack — one-click mechanic — customer, shop, and date already known — single confirm button — optional discount if taking immediately — or possibility to order for someone else — falls back to default weekend pack if no specific pack assigned — if today NOT within active days: skip directly to thank-you screen [5.4] Digiteal — Payment Integration Documentation: https://doc.digiteal.eu/reference/createpaybutton Pay Button:  GET /createpaybutton  — passes order amount, reference, creditor details — customer redirected to Digiteal On completion: webhook ( POST ) confirms order status — back office updated in real-time via  POST /setwebhookconfiguration Office Shop company billing:  POST /createinvoicepaymentrequest  — invoice sent to company Multi-Day grouped order: single Digiteal payment request covering total of all day-orders — webhook links payment confirmation back to each sub-order Refund:  POST /refund  — accessible per order (or per grouped order) in the franchisee order management panel Payment fails: clear error shown — order held in pending — customer can retry All status transitions (pending → paid → failed → refunded) reflected in back office in real-time PHASE 6 — SEO   After storefront is stable and all content is in place [6.1] SEO Agent — Multilanguage For each active language configured in the store: Page titles and meta descriptions — shop home, each category page, each product Alt text for all product and category images Structured data (schema.org): Product, BreadcrumbList, LocalBusiness per franchise store Canonical URLs per language — sitemap updated with all language variants (hreflang tags) Slug/URL structure per language — clean, keyword-rich, no auto-generated IDs in URLs Keywords: artisan bakery, fresh bread, pastries, office delivery, click and collect, local bakery — adapted per language with locally relevant equivalents A non-registered user must be able to browse and build a basket freely — no account required to add products to the cart. Registration or login is only triggered at the moment they choose to validate/checkout their basket — not before. Flow for non-registered user: Browse freely — add products to cart — no obstacle Click checkout / validate basket First step shown:  Create an account or log in  — registration form (first name, last name, email, phone, password) or login option Once registered or logged in: checkout continues directly from the next step (Delivery → Payment) — no need to re-enter their details The cart is preserved through the registration/login step — items are not lost when the customer creates their account. The VIES validate button is too large/prominent for its context. Restyle it to be compact and inline with the VAT number input field — same height as the input, tight width, minimal visual weight. It should feel like a secondary action attached to the field, not a primary CTA button. Perform a full codebase audit and refactor. After all the recent feature additions and bug fixes, the code has accumulated technical debt. Clean and optimize the entire codebase without changing any functionality: Frontend: Remove all unused components, props, and CSS classes Consolidate duplicate component logic — the Shop creation and Edit forms must share a single form component — any other duplicated UI patterns must be unified Ensure all color, font, and spacing values reference CSS variables — remove any remaining hardcoded values Remove all console.log statements and debug code Audit all string literals — any user-facing string not connected to the translation system must be flagged and connected Ensure the product card, basket, checkout, and order confirmation all use the same shared components and do not duplicate layout logic Backend / API: Remove unused routes and dead code Consolidate duplicate query logic into shared service functions Ensure all API responses follow a consistent structure:  { data, meta, error } Validate that all required fields are enforced server-side — not just client-side Ensure the order state machine is cleanly defined and enforced in one place:  pending → paid / failed / pending_pay_at_store → paid / cancelled Confirm all Digiteal webhook handlers are idempotent — processing the same webhook twice must not create duplicate state changes Remove any direct database queries from route handlers — all logic through service layer Data &amp; State: Audit the basket state — ensure Webshop and Office Shop basket sections are managed by a single clean state manager — no duplicated state Confirm the date lock mechanism (basket non-empty → date picker locked) is enforced in one place and not scattered across components Ensure the progressive selection logic (no shop / no date / both selected) is a single state machine — not multiple conditional checks spread across files Performance: Lazy load components that are not needed on the landing page Ensure the product grid does not re-render on every keystroke or minor state change — memoize where appropriate API calls for product availability should be debounced — not fired on every state change Goal: faster load, cleaner code, zero functional regression.